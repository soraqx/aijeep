opencv-python>=4.8
# Windows wheels from ~0.10.30 onward are Tasks-only (no mediapipe.solutions / Face Mesh). Use last cp311 wheel with Solutions API.
mediapipe
protobuf>=3.20,<5
scikit-learn
pyserial
requests
huggingface_hub
